// cpp is case sensitive
#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int base, component;
    cout << "Enter the base";
    cin >> base;
    cout << "ENter the power componemt";
    cin >> component;
    cout << pow(base, component);
}